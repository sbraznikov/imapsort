# Example

from imapsort import Imapsort

server = 'my.email.server'
user = 'my_user_name'
password = 'my_password'

rules = (
    ('from', 'foo@bar'),
    ('action', ('move', 'INBOX.foo)),
    ('from', 'bar@foo'),
    ('action', ('move', 'INBOX.bar')),
)

Imapsort(server, user, password, rules)
